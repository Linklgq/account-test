<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.lenovo.logintest">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!--<uses-permission android:name="android.permission.READ_LOGS" />-->

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="SMS_MOB_APPKEY"
            android:value="2873dedb9e470"/>
        <meta-data
            android:name="SMS_MOB_APPSECRET"
            android:value="4ad8b201a116cb362c511a474e0e3868"/>

        <activity android:name=".account.login.LoginActivity">
        </activity>
        <activity android:name=".account.register.RegisterActivity">
        </activity>
        <activity android:name=".account.resetpwd.ResetPasswordActivity">
        </activity>
    </application>

</manifest>